name: minirox CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 21 * * *"
  workflow_dispatch:

jobs:
  test:
    if: >-
      (
        (
          github.event_name == 'schedule'
          && github.repository == 'minirox/minirox'
        ) || (
            github.event_name != 'schedule'
            && !(
              contains(github.event.head_commit.message, '[ci skip]')
              || contains(github.event.head_commit.message, '[skip ci]')
            )
        )
      )
    runs-on: ubuntu-latest
    container: dolfinx/dolfinx
    strategy:
      matrix:
        petsc_arch: [real, complex]
      fail-fast: false
    steps:
      - uses: actions/checkout@v2
      - name: Set PETSc scalar type
        run: |
          . /usr/local/bin/dolfinx-${{ matrix.petsc_arch }}-mode
          echo "PETSC_ARCH=$PETSC_ARCH" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH=$LD_LIBRARY_PATH" >> $GITHUB_ENV
          echo "PYTHONPATH=$PYTHONPATH" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=$PKG_CONFIG_PATH" >> $GITHUB_ENV
      - name: Install dependencies
        run: |
          pip3 -q install flake8==3.9.2
          pip3 -q install --upgrade numpy
          pip3 -q install -r requirements.txt -r tests/requirements.txt -r tutorials/requirements.txt
          PYVISTA_INIT=$(python3 -c 'import os, pyvista; print(os.path.join(os.path.dirname(pyvista.__file__), "__init__.py"))')
          sed -i "s/os.makedirs(USER_DATA_PATH)/os.makedirs(USER_DATA_PATH, exist_ok=True)/g" ${PYVISTA_INIT}
      - name: Install minirox
        run: |
          pip3 install .
      - name: Run flake8 checks
        run: |
          pytest --flake8 -m flake8
          pytest --flake8 -m flake8 .github
      - name: Check for stray outputs, counts and metadata in ipynb files
        uses: ./.github/actions/jupyter/check_metadata
      - name: Run documentation generation
        run: |
          cd docs && make html
      - name: Run unit tests (serial)
        run: |
          pytest -n auto --cov=minirox tests/unit
      - name: Upload coverage report as an artifact
        uses: actions/upload-artifact@v2
        with:
          name: coverage-${{ matrix.petsc_arch }}
          path: .coverage
          retention-days: 1
      - name: Run unit tests (parallel)
        run: |
          mpirun -n 2 pytest tests/unit
      - name: Run tutorials (serial)
        run: |
          pytest -n auto tutorials

  combine_coverage_reports:
    runs-on: ubuntu-latest
    container: ubuntu
    needs: [test]
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          apt-get -qq update
          export DEBIAN_FRONTEND="noninteractive"
          apt-get install -qq python3-pip
          pip3 -q install coverage
      - name: Download coverage reports from artifacts
        uses: actions/download-artifact@v2
      - name: Combine coverage reports
        run: |
          coverage combine coverage-real/.coverage coverage-complex/.coverage
          coverage report --fail-under=100 --show-missing --skip-covered
